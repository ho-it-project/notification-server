describe('AppController', () => {
  describe('init', () => {
    it('should return true', () => {
      expect(true).toBe(true);
    });
  });
  describe('health', () => {
    it('should return OK', () => {
      expect('OK').toBe('OK');
    });
  });
});
